# ecpl-group-recon

Static data + simple web UI for cross-party reconciliation (Space Within, ECPL, STPL, Nakul).

## Data

Place simplified CSV/JSON snapshots under `data/` with a dated subfolder when you refresh exports.

## Status

Project initialized for GitHub; wire the app and copy tagged masters from `Downloads\ORIGINALS` when ready.
